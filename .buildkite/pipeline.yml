steps:
  - label: ":shell: Tests"
    plugins:
      docker-compose#v1.2.1:
        run: tests

  - label: ":shell: Shellcheck"
    plugins:
      shellcheck#v1.0.1:
        files: hooks/**

  - label: ":sparkles: Lint"
    plugins:
      plugin-linter#v1.0.0:
        name: docker-login